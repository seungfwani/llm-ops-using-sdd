# 🧩 **SDD 개발 헌법(Constitution)**

## 1. 문서 개요
### 1.1 목적
- 본 문서는 ○○ 시스템/모듈의 설계 상세를 정의하여 일관된 개발 및 유지보수 기준을 제공하는 것을 목적으로 한다.

### 1.2 범위
- 이 문서가 다루는 기능, 모듈, 서비스 영역을 정의한다.

### 1.3 참고 문서
- 요구사항 명세서(URS/FRS)
- 아키텍처 문서(AD)
- API 명세서
- 기타 관련 문서

---

## 2. 시스템 개요
### 2.1 시스템 구성도
- 전체 시스템 구조 다이어그램 (Component Diagram / Topology 포함)

### 2.2 주요 컴포넌트 설명
- 각 컴포넌트의 역할, 책임, 연결관계 정의

### 2.3 데이터 흐름도(DFD)
- 주요 처리 흐름과 데이터 흐름 설명

---

## 3. 기능 설계(Functional Design)
### 3.1 기능 목록
- **CAT-001**: 모델 카탈로그 등록 - 연구자가 모델 메타데이터와 모델 카드를 등록하고 버전을 생성
  - 입력: 모델명, 버전, 타입(base/fine-tuned/external), 소유팀, 메타데이터(JSON), 데이터셋 lineage
  - 출력: 생성된 모델 엔트리 ID, 상태(draft/under_review/approved/deprecated)
- **CAT-002**: 데이터셋 버전 관리 - 데이터 엔지니어가 데이터셋을 업로드하고 PII/품질 검사를 수행
  - 입력: 데이터셋명, 버전, 저장소 URI, 변경 로그
  - 출력: 데이터셋 레코드 ID, PII 스캔 상태, 품질 점수(0-100), 승인 여부
- **CAT-003**: 승인 워크플로우 - 리뷰어가 모델/데이터셋 엔트리를 검토하고 승인/거부 결정
  - 입력: 엔트리 ID, 버전, 결정(approve/reject/request_changes), 메모
  - 출력: 업데이트된 상태, 승인/거부 타임스탬프, 감사 로그 항목
- **CAT-004**: 카탈로그 조회 및 비교 - 사용자가 모델/데이터셋 목록을 조회하고 버전 간 차이를 비교
  - 입력: 필터 조건(팀, 상태, 타입), 정렬 옵션
  - 출력: 엔트리 목록, 버전 diff 요약, lineage 그래프
- **TRN-001**: 파인튜닝/분산 학습 작업 제출 - ML 엔지니어가 GPU 리소스 요구사항과 하이퍼파라미터를 지정하여 학습 작업을 제출
  - 입력: 모델 ID, 데이터셋 ID, 작업 타입(finetune/distributed), 리소스 프로파일(GPU 수/타입, 최대 지속시간), 하이퍼파라미터, 재시도 정책
  - 출력: 작업 ID, 스케줄러 ID, 상태(queued/running/succeeded/failed/cancelled), 제출/시작/완료 타임스탬프
- **TRN-002**: 실험 메트릭 기록 - 학습 중 실시간으로 메트릭(손실, 정확도 등)을 기록하고 MLflow 호환 저장소에 저장
  - 입력: 작업 ID, 메트릭명, 값, 단위
  - 출력: 메트릭 레코드 ID, 기록 타임스탬프
- **TRN-003**: 작업 상태 조회 및 취소 - 사용자가 실행 중인 작업의 상태를 조회하거나 큐에 대기 중인 작업을 취소
  - 입력: 작업 ID
  - 출력: 작업 상태, 실험 URL, 메트릭 요약
- **SRV-001**: 서빙 엔드포인트 배포 - 승인된 모델을 Kubernetes에 배포하고 HPA, Ingress 설정
  - 입력: 모델 ID, 환경(dev/stg/prod), 라우트 경로, 최소/최대 레플리카 수, 오토스케일 정책
  - 출력: 엔드포인트 ID, 상태(deploying/healthy/degraded/failed), 배포 UID, 롤백 계획
- **SRV-002**: 프롬프트 A/B 실험 실행 - 두 프롬프트 템플릿 간 트래픽 분할 및 메트릭 수집
  - 입력: 템플릿 A ID, 템플릿 B ID, 할당 비율(%), 최적화 메트릭(지연시간/사용자 만족도 등)
  - 출력: 실험 ID, 시작/종료 시간, 승리 템플릿 ID
- **SRV-003**: 서빙 엔드포인트 롤백 - 배포 실패 또는 성능 저하 시 이전 버전으로 복구
  - 입력: 엔드포인트 ID
  - 출력: 롤백 성공 여부, 업데이트된 상태
- **SRV-004**: 서빙 엔드포인트 목록 조회 및 상세 보기 - 배포된 엔드포인트 목록 조회 및 필터링, 상세 정보 확인
  - 입력: 필터 조건(환경, 모델 ID, 상태), 엔드포인트 ID
  - 출력: 엔드포인트 목록 또는 상세 정보(라우트, 상태, 스케일링 설정, 바인딩된 모델 메타데이터)
- **GOV-001**: 거버넌스 정책 생성 및 관리 - 관리자가 정책을 생성하고 활성화/비활성화
  - 입력: 정책명, 스코프(catalog/training/serving/global), 규칙(JSON), 상태(draft/active/deprecated)
  - 출력: 정책 ID, 생성/검토 타임스탬프
- **GOV-002**: 정책 평가 및 RBAC 강제 - 모든 API 요청에 대해 정책 평가 수행
  - 입력: 스코프, 액션, 리소스 타입, 사용자 역할, 메타데이터
  - 출력: 허용/거부 결과, 거부 사유, 정책 ID
- **GOV-003**: 감사 로그 조회 - 운영자가 모든 시스템 액션의 감사 로그 조회
  - 입력: 액터 ID, 리소스 타입, 액션, 시간 범위, 제한 수
  - 출력: 감사 로그 목록(액터, 액션, 리소스, 결과, 타임스탬프)
- **GOV-004**: 비용 프로파일 생성 및 집계 - 백그라운드 워커가 학습/서빙 리소스의 비용 집계
  - 입력: 리소스 타입, 리소스 ID, 시간 윈도우, GPU 시간, 토큰 수, 비용 금액
  - 출력: 비용 프로파일 ID, 집계된 총 비용, GPU 시간, 토큰 수

### 3.2 시나리오/플로우
**시나리오 1: 모델 등록 및 승인**
1. 연구자가 `/llm-ops/v1/catalog/models` POST로 모델 엔트리 생성 (상태: draft)
2. 시스템이 필수 메타데이터 검증 수행
3. 연구자가 "Request Approval" 액션 트리거 (상태: under_review)
4. 리뷰어가 `/llm-ops/v1/catalog/models/{id}/versions/{v}/approve` POST로 승인 결정
5. 시스템이 상태를 approved로 업데이트하고 감사 로그 기록

**시나리오 2: 데이터셋 품질 검사**
1. 데이터 엔지니어가 데이터셋 업로드 및 버전 생성
2. 백그라운드 워커가 PII 스캔 및 품질 점수 계산 실행
3. 품질 점수가 임계값 미만이면 승인 차단
4. 품질 점수 통과 시 승인 가능 상태로 전환

**시나리오 3: 파인튜닝 작업 제출 및 실험 추적**
1. ML 엔지니어가 `/llm-ops/v1/training/jobs` POST로 작업 제출 (모델 ID, 데이터셋 ID, GPU 요구사항, 하이퍼파라미터)
2. 시스템이 모델/데이터셋 승인 상태 검증 후 Kubernetes 스케줄러에 작업 제출
3. Kubernetes가 GPU 노드에 Pod 할당 및 학습 시작 (상태: running)
4. 학습 컨테이너가 실시간으로 메트릭을 `/llm-ops/v1/training/jobs/{id}/metrics` POST로 전송
5. 실험 로깅 워커가 메트릭을 MLflow 호환 저장소에 기록
6. 작업 완료 시 상태를 succeeded/failed로 업데이트하고 실험 URL 생성
7. 실패 시 재시도 정책에 따라 자동 재시도 (최대 3회)

**시나리오 4: 작업 실패 및 복구**
1. 학습 작업이 Kubernetes에서 실패 (상태: failed)
2. 시스템이 재시도 정책 확인 후 자동 재시도 또는 사용자에게 알림
3. 사용자가 `/llm-ops/v1/training/jobs/{id}` GET으로 실패 원인 조회 (로그, 메트릭)
4. 사용자가 수정된 하이퍼파라미터로 새 작업 제출 또는 기존 작업 취소

**시나리오 5: 서빙 엔드포인트 배포**
1. ML 엔지니어가 `/llm-ops/v1/serving/endpoints` POST로 승인된 모델 배포 요청
2. 시스템이 모델 승인 상태 검증 및 라우트 중복 확인
3. Kubernetes에 Deployment, Service, HPA, Ingress 생성
4. 헬스 체크 통과 시 상태를 healthy로 업데이트
5. 실패 시 상태를 failed로 업데이트하고 롤백 계획 저장

**시나리오 6: 프롬프트 A/B 실험**
1. 프롬프트 엔지니어가 `/llm-ops/v1/prompts/experiments` POST로 A/B 실험 생성
2. 시스템이 템플릿 A와 B를 할당 비율에 따라 라우팅 (사용자 ID 기반 일관성 유지)
3. 실험 중 메트릭(지연시간, 사용자 만족도 등) 수집
4. 실험 종료 시 승리 템플릿 결정 및 서빙 엔드포인트에 적용

**시나리오 7: 서빙 엔드포인트 롤백**
1. 서빙 엔드포인트에서 성능 저하 또는 오류 발생 감지
2. 사용자가 `/llm-ops/v1/serving/endpoints/{id}/rollback` POST로 롤백 요청
3. 시스템이 이전 배포 버전으로 복구 (Deployment 스케일 다운, 이전 버전 재배포)
4. 롤백 완료 후 상태 업데이트 및 감사 로그 기록

**시나리오 7a: 서빙 엔드포인트 목록 조회 및 상세 보기**
1. 사용자가 `/llm-ops/v1/serving/endpoints` GET으로 엔드포인트 목록 조회 (선택적 필터: environment, modelId, status)
2. 시스템이 필터 조건에 맞는 엔드포인트 목록 반환 (라우트, 환경, 상태, 모델 ID, 레플리카 수 포함)
3. 사용자가 목록에서 특정 엔드포인트 선택하여 `/llm-ops/v1/serving/endpoints/{id}` GET으로 상세 정보 조회
4. 시스템이 엔드포인트 상세 정보 반환 (라우트, 헬스 상태, 스케일링 설정, 바인딩된 모델 메타데이터)
5. 사용자가 UI에서 필터를 변경하여 원하는 엔드포인트만 조회 가능

**시나리오 8: 거버넌스 정책 위반 및 차단**
1. 사용자가 `/llm-ops/v1/catalog/models` DELETE로 모델 삭제 시도
2. RBAC 미들웨어가 사용자 역할 확인 (예: "researcher")
3. 정책 평가 서비스가 "catalog" 스코프의 활성 정책 평가
4. 정책 규칙에 "delete" 액션에 "admin" 역할 필요로 정의되어 있음
5. 정책 평가 실패 → HTTP 403 반환 및 감사 로그 기록 (result: denied)
6. 사용자에게 거부 사유 표시

**시나리오 9: 비용 집계 및 이상 탐지**
1. 비용 집계 워커가 24시간마다 실행
2. 완료된 학습 작업의 GPU 시간 및 비용 계산
3. 활성 서빙 엔드포인트의 토큰 소비량 집계
4. 비용 프로파일 생성 및 저장
5. Grafana 대시보드에 비용 메트릭 표시
6. 비용 이상(1시간 내 $1000 초과) 감지 시 Prometheus 알림 발송

### 3.3 화면 설계(해당 시)
- **ModelList.vue**: 모델 목록 페이지 - 테이블 형태로 모델명/버전/상태/소유팀 표시, 필터링/정렬, "Create Model" 버튼
- **ModelDetail.vue**: 모델 상세 페이지 - 메타데이터 표시, lineage 그래프, 버전 히스토리, 승인 워크플로우 버튼
- **DatasetList.vue**: 데이터셋 목록 페이지 - 데이터셋명/버전/PII 상태/품질 점수 표시, 품질 리포트 링크
- **JobSubmit.vue**: 학습 작업 제출 페이지 - 모델/데이터셋 선택, GPU 리소스 설정, 하이퍼파라미터 입력, 작업 타입 선택
- **JobDetail.vue**: 학습 작업 상세 페이지 - 작업 상태, 제출/시작/완료 시간, 실험 메트릭 차트, 실험 URL 링크, 취소 버튼
- **EndpointList.vue**: 서빙 엔드포인트 목록 페이지 - 테이블 형태로 엔드포인트 ID/라우트/환경/상태/모델 ID/레플리카 수 표시, 필터링(환경/모델/상태), 상세 페이지로 이동
- **EndpointDetail.vue**: 서빙 엔드포인트 상세 페이지 - 기본 정보(라우트, 환경, 상태), 모델 정보, 스케일링 설정(최소/최대 레플리카), 롤백 버튼
- **EndpointDeploy.vue**: 서빙 엔드포인트 배포 페이지 - 모델 선택, 환경(dev/stg/prod) 선택, 라우트 경로 입력, 레플리카 수 설정, 오토스케일 정책 설정
- **ExperimentCreate.vue**: 프롬프트 A/B 실험 생성 페이지 - 템플릿 A/B 선택, 할당 비율 설정, 최적화 메트릭 선택
- **PolicyList.vue**: 거버넌스 정책 목록 페이지 - 정책명/스코프/상태 표시, 필터링(스코프/상태), "Create Policy" 버튼
- **AuditLogs.vue**: 감사 로그 페이지 - 타임스탬프, 액터, 액션, 리소스, 결과 표시, 필터링(액터/리소스 타입/액션)
- **CostDashboard.vue**: 비용 대시보드 페이지 - 총 비용, GPU 시간, 토큰 수, 리소스별 비용 프로파일 표시, 시간 범위 필터

---

## 4. 기술 설계(Technical Design)
### 4.1 모듈 구조
- 패키지 구조도
- 클래스/모듈 다이어그램

### 4.2 인터페이스/API
- API Endpoint
    - API prefix: `/llm-ops/v1`
- Request/Response 정의
    - Response 구조
        - 모든 API 이 시스템의 문제(버그) 없이 동작하면 200 OK 로 리스폰스 된다.
        - API 동작에서 발생한 문제는 Response Body 에 메시지로 넣는다.
        - {"status": "success/fail", "message": "실패시 메시지 전달", "data": 결과 데이터 전달 }
- Error Code 정의
    - 서버에서는 모든 Exception 을 잡아서 200 ok 를 내보내야한다.
    - API 요청에서 발생한 문제는 4xx 사용
    - API 서버에 문제가 생긴 경우 5xx 사용
- RBAC / Policy Middleware
    - 모든 요청은 `X-User-Id`, `X-User-Roles` 헤더를 포함해야 하며 미들웨어에서 정책 검증을 통과해야 한다.
    - 정책 엔진은 `docs/` 내 거버넌스 규칙 변경 시 업데이트되어야 한다.
- 관측/로그 설계
    - 요청 카운터 및 지연시간 히스토그램을 Prometheus에 기록한다.
    - 구조화된 로그 포맷을 정의하고 각 서비스에 적용한다.
- 외부 클라이언트 연동
    - Redis, 객체 스토리지(MinIO/S3 호환) 클라이언트 구성이 명시되어야 한다.

### 4.3 DB 설계
- ERD
- 테이블 정의서(Schema)
- 인덱스 전략

### 4.4 프로세스 처리 방식
- 비동기/동기 처리 여부
- 큐, 배치, 고속병렬파일시스템 활용 여부(요청 시 반영 가능)

---

## 5. 비기능 설계(Non-functional Design)
### 5.1 성능 요구사항
- 처리량, 지연시간, 동시성 요구사항

### 5.2 보안 설계
- 인증, 권한, 암호화, 데이터 보호 정책

### 5.3 장애 처리 및 복구 전략
- 장애 시 처리 흐름
- 재시도/롤백 전략

### 5.4 로깅 및 모니터링
- 로그 레벨 정책
- Metric/Tracing 정의

---

## 6. 배포 및 운영
### 6.1 배포 구조
- Deployment Diagram
- 환경별 구성 정보(DEV / STG / PROD)

### 6.2 운영 정책
- 백업 정책
- 스케일링 정책
- 유지보수 프로세스

---

## 7. 용어 정의
- 본 문서에서 사용하는 주요 용어와 약어 정의
